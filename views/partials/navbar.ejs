<nav class="navbar navbar-expand-lg navbar-dark fixed-top" style="background-color: #1a1a2e;">
  <div class="container">

    <!-- Brand -->
    <a class="navbar-brand fw-bold fs-4" href="/">
      <span style="color: #e94560;">Smart</span>Events
    </a>

    <!-- Mobile Toggler -->
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Nav Links -->
    <div class="collapse navbar-collapse" id="mainNav">
      <ul class="navbar-nav ms-auto align-items-center gap-1">

        <!-- Always visible -->
        <li class="nav-item">
          <a class="nav-link px-3" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link px-3" href="/enquiries/contact">Contact</a>
        </li>

        <% if (user) { %>

          <!-- Logged in: show dashboard -->
          <li class="nav-item">
            <a class="nav-link px-3" href="/bookings/dashboard">Dashboard</a>
          </li>

          <!-- Admin only: show event management -->
          <% if (user.role === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link px-3" href="/events/manage">Manage Events</a>
            </li>
          <% } %>

          <!-- Logged in: show username and logout -->
          <li class="nav-item ms-2">
            <span class="navbar-text text-warning me-3">
              <i class="bi bi-person-circle me-1"></i><%= user.name %>
            </span>
          </li>
          <li class="nav-item">
            <a class="btn btn-outline-light btn-sm px-3" href="/logout">Logout</a>
          </li>

        <% } else { %>

          <!-- Not logged in: show login and register -->
          <li class="nav-item ms-2">
            <a class="nav-link px-3" href="/login">Login</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-warning btn-sm px-3 fw-semibold" href="/register">Register</a>
          </li>

        <% } %>

      </ul>
    </div>

  </div>
</nav>